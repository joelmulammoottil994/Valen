import {Ordercard as Component} from './Ordercard';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Ordercard',
  component: Component,
};

export const Ordercard: Story = {
  ,
};

export default meta;
